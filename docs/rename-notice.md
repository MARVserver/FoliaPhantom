# Rename Notice: FoliaPhantom to pasta

FoliaPhantom has been renamed to **pasta**.

This change affects user-facing names, documentation, and release files. Internal Java packages and Maven module directory names may keep the old `foliaphantom` identifiers temporarily to avoid breaking builds during the transition.

Use pasta release artifacts for new installs and upgrades.
